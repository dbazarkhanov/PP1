#include <iostream>
#include <algorithm>
using namespace std;
int main(){
    string s;
    string n;
    cin >> s >> n;
    for(int pos=1; pos<=s.size_t(); pos++)
    cout << n.erase(s.find(0, pos));
    return 0;
}